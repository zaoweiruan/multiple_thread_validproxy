# Current Context

> **角色**: 提供跨会话的上下文锚点。每轮会话开始时，Agent 应检查并更新 §三。
> **维护规则**: §三 在每轮会话结束时更新：移除已完成的旧条目，添加本会话的 Bug 修复引用。
> **边界**: 只记录"在哪里"和"最近做了什么"的上下文锚点，不记录深度知识（归档至 `docs/project-knowledge.md`）。

---

## 一、基于 xray、v2rayn 开源项目

- **v2rayn 源码目录**: `E:\eclipse_workspace\v2rayn`
  - 关键文件:
    - `v2rayN\ServiceLib\Handler\ConfigHandler.cs` — DedupServerList() 方法
    - `v2rayN\ServiceLib\Models\ProfileItem.cs` — 数据模型
    - `v2rayN\ServiceLib\Manager\AppManager.cs` — ProfileItems() 获取代理列表
- **xray 源码目录**: `E:\eclipse_workspace\Xray-core`
- **wxWidgets 源码目录**: `E:\eclipse_workspace\wxWidgets`
  - 关键文件:
    - `src/generic/datavgen.cpp:2091` — wxDataViewMainWindow Create() 缺失 CS_DBLCLKS 导致 MSW 平台双击无效

## 二、项目配置文件

- `E:\eclipse_workspace\multiple_thread_validproxy\bin\config.json`

## 三、本会话引用

> 每轮会话开始时，将前一轮的已关闭条目移至 `docs/project-knowledge.md §7` 或 `docs/bugfix/`。
> 格式: 条目简述 → `docs/bugfix/YYYY-MM-DD-xxx.md`

| 日期 | 条目 | 文档 |
|------|------|------|
| 2026-08-24 | **即时订阅切换后其它订阅显示为空（缓存误存过滤子集）** — 即时切换实现（同日 Spec）误假设 ProxyListLoadedEvent 携带全表，实际 `loadProxiesAsync` 后台线程 dao.getAll() 全表经 copy_if 过滤后事件只携子集且全表即弃 → 面板 allProxies_ 仅含首个订阅行，切换其它订阅内存过滤 0 命中空列表；修复=AppController.cpp 删除 copy_if 分支让事件直接携带 getAll() 全表（零额外 IO/内存）+ ProxyListPanel.cpp 4 参 loadProxies 尾部视图逻辑委托 applySubscriptionFilter 复用；验证=构建 0 error + ctest 32/32 | `docs/bugfix/2026-08-24-Bugfix-InstantSubSwitch-CacheSubset-v1.0.md` |
| 2026-08-24 | **订阅切换零 DB 内存过滤（ProxyListPanel 白屏阻塞修复）** — 根因=每次订阅切换 MainFrame:282 无条件启动 `loadProxiesAsync` 后台线程全表读取（profiles 5.3 万行 + 全量 ProfileExItem + buildProxyListMaps），延迟 O(全库) 与所选订阅记录数无关，0 记录订阅同样白屏等待，快速连续切换堆叠 detached 线程争抢 IO 加重阻塞；而面板已缓存全集数据（allProxies_/exItems_/预构建 maps），同文件 filterBySearch 已验证纯内存过滤即时可用；修复=新增 `ProxyListPanel::applySubscriptionFilter(subId)`：`cacheReady_` 未就绪回退 `reloadFromDatabase()` 异步路径，就绪则从 allProxies_ 按 subid 内存过滤 + `setDataWithoutRebuild`（maps 按 indexId 键控与过滤无关，免 O(N) rebuildMaps）+ 双 Reset + detectIdOffset 镜像 updateProxyList 视图语义；MainFrame SubscriptionSelectedEvent 改调该方法；其余显式刷新入口保留异步全量刷新顺带维持缓存新鲜；留档=stale-generation 乱序防护范围外、filterBySearch 的 rebuildMaps 开销另评；验证=构建 0 error + ctest 32/32 | `docs/specs/2026-08-24-Spec-ProxyListPanel-InstantSubSwitch-v1.0.md` |
| 2026-08-24 | **可配置悬浮窗监控独立代理进程（StandaloneFloatingWidget）v1.1 统一接管版 — 方案评审稿** — 悬浮窗**替代 StandaloneMonitorDialog**成为「监控代理进程」唯一 UI；配置**零新增键**（复用 `proxy_process_monitor.enabled`+`checkIntervalMs`），**废弃 `targetProcessNames`**（状态栏 `proxyMonPanel_` 计数改由 `getRunningStandaloneCount()` 同源驱动）；折叠条常驻总数 + **鼠标悬停展开**详情表（IndexId/Host/时长/端口/PID），双击行经 LocateProxyEvent 定位；Ctrl+M/工具栏/菜单重定向 `toggleActive()`；透明度/置顶/悬停硬编码；含 `targetProcessNames` 全仓清理核查清单；**状态=📝 draft 待评审，未实施代码（v1.0 已废弃）** | `docs/specs/2026-08-24-Spec-StandaloneFloatingWidget-v1.1.md` |
| 2026-08-21 | **代理监控窗口双击行定位到代理列表（LocateProxyEvent）** — 新增 `LocateProxyEvent`（`wxEVT_LOCATE_PROXY`，携带 indexId）复用既有自定义事件机制：`StandaloneMonitorDialog::onItemActivated`（EVT_LIST_ITEM_ACTIVATED 取第0列 indexId → `wxQueueEvent(GetParent(), ...)` → `Show(false)` 隐藏）+ `MainFrame::Bind` 调 `proxyPanel_->selectProxyByIndexId`；复用 `ProxyListPanel::selectProxyByIndexId`（已具备，无 MessageBox 干扰），不触及 FOUND: 字符串通道；遵循 StandaloneProxyEvent/RunningDurationsLoadedEvent 解耦模式；验证=构建 0 error + ctest 32/32 | `docs/specs/2026-08-21-Spec-StandaloneMonitor-DoubleClickLocate-v1.0.md` |
| 2026-08-21 | **ProxyListPanel 列排序模型列 vs 视觉位置修复** — `wxDataViewEvent::GetColumn()` 返回模型列索引，而 `wxDataViewCtrl::GetColumn(pos)` 需视觉位置；列重排后二者不一致致 Latency/Health/Starts/Runtime 点击排序映射到错误列（重排前视觉位置==模型列巧合可用，重排后发散）；修复=新增 `resolveColumnByModel(int)`（遍历视觉位置按 `GetModelColumn()` 匹配，不依赖 wx 内部 API）替换 3 处 `GetColumn(dvEvent.GetColumn())`（ProxyListPanel.cpp:334、:697；SubscriptionPanel.cpp:524）；验证=构建 0 error + ctest 32/32（含 test_proxy_list_model 15/15） | `docs/bugfix/2026-08-21-Bugfix-ProxyListPanel-ColumnSortModelVsVisual-v1.0.md` |
| 2026-08-21 | **ProxyListPanel 列显示顺序调整（用户指定顺序）** — 仅重排 `onColumnsInit` 的 AppendTextColumn 调用顺序（Region / Latency ↕ / Health ↕ / Type / Host ↕ / Port / Message ↕ / Starts ↕ / Runtime ↕ / # / IndexId / Failures ↕ / Remarks）；ProxyListModel 未动（排序基于模型列索引，框架 Resort 时 Compare 收到 GetModelColumn() 与视觉顺序解耦）；构建 0 error + ctest 32/32 | `docs/specs/2026-08-21-Spec-ProxyListPanel-ColumnReorder-v1.0.md` |
| 2026-08-21 | **工具栏新增「监控代理」按钮（打开独立代理监控对话框）** — 新增 ID `ID_TOOL_STANDALONE_MON` 复用 `onMenuStandaloneMonitor`（懒创建+Show+Raise），插入「配置」按钮左侧；图标 `tool_monitoring_proxy_process.png` 经 `icons.rc` 嵌入为 `tool_monitoring_proxy_process_png` 资源 + `bin/icons/` 回退；构建 0 error + ctest 32/32 | `docs/specs/2026-08-21-Spec-Toolbar-MonitorProxyButton-v1.0.md` |
| 2026-08-21 | **独立代理由其它程序启动且配置文件在其它目录时无法解析监听端口修复** — 纳管仅双目录探测 `<exeDir>\config\`/`<exeDir>\worker\config\`；外部程序把 standalone config 放在任意目录时文件找不到→`readStandaloneInboundPort` 返回 0→端口恒 0；修复=新增 `ProcessInspector::extractConfigFullPath`（命令行提取含目录完整路径）+ 纳管端口解析第三步回退直读端口；权威无歧义（规避 OS 套接字查询多 inbound 端口错配，尤其 sing-box 多个 DNS inbound）；TDD `test_process_inspector` 9 用例 + CMake 注册；验证=构建 0 error + ctest 32/32 | `docs/bugfix/2026-08-21-Bugfix-StandaloneProxy-PortExternalConfigDir-v1.0.md` |
| 2026-08-21 | **独立代理监控弹窗（StandaloneMonitorDialog）+ watch 回填/端口回填两处修复** — 新增非模态弹窗实时展示正在 watch 的代理进程会话数据（索引ID/起始时间/运行时长(分)/监听端口/PID），2 秒 wxTimer 刷新，Proxy 菜单「独立代理监控…Ctrl+M」入口（懒创建+隐藏复用）；数据链=`AppController::getWatchedStandaloneMonitors()` 锁内快照 standaloneProxies_ (running&&managed) ∪ `ProxyRuntimeHistoryDAO::getInProgressSessions()`（新方法，ProxyRuntimeHistoryItem 补 pid 字段）按 historyId 联结、时长实时计算 now−started_at；**顺带修复**①正常启动路径未回填 runtimeHistoryId/watchKey/startedAt 至 map 致 stopStandaloneProxy 跳过 unwatch（watcher 线程泄漏）；②悬垂纳管进程 socksPort 恒 0——新增 `readStandaloneInboundPort()` 解析 config 文件 inbound 端口回填 + 纳管/停止日志追加端口；测试=TDD 新增 DAO GetInProgressSessions 3 用例（含排除 finalized 行/空表）+ 构建 0 error + ctest 31/31 | `docs/specs/2026-08-21-Spec-StandaloneMonitor-Dialog-v1.0.md` |
| 2026-08-21 | **配置编辑窗口移除 ipinfo.io Token 项（Phase 1 UI 移除 + Phase 2 死代码清理）** — Region 解析经两次迁移（ipinfo.io→ip-api.com→ipwho.is）后 `config.test.ipinfo_token` 已无任何代码消费者；Phase 1=ConfigDialog.cpp 移除 3 处 UI 触点（属性创建/加载赋值/保存读取）；Phase 2（用户确认后执行）=删除 ConfigReader.h:19 字段声明 + TestConfigParser.h:35-39 解析分支 + ConfigJsonSerializer.cpp:26-28 写回 + ConfigDialog 过时注释；行为变化=旧 config.json 中该键静默忽略，下次通过配置窗口保存后从文件消失；验证=构建 0 error + ctest 31/31 ×2 轮 + grep 源码无功能引用残留 | `docs/specs/2026-08-21-Spec-ConfigDialog-RemoveIpinfoToken-v1.0.md` |
| 2026-08-21 | **解析/批量解析完成后 ProxyListPanel 自动刷新** — Region 存储于 ProfileItem（proxies_），但解析完成事件链路最终只调 `refreshResults()`（语义为仅重载 exItems_ 测试结果列），Region 列永不更新；修复=遵循既有 StatusUpdateEvent 字符串命令模式（先例 DEDUP_OK/RESOLVE_REGION_START）新增 `REGION_RESOLVE_DONE`：批量（doResolveRegionsBatch）与单代理（doResolveSingleProxyRegion）完成时向顶层窗口广播，MainFrame::onStatusUpdate 新增分支调 `proxyPanel_->reloadFromDatabase()`（新公有方法，经 loadProxiesAsync 异步全量重载+预构建 maps，不阻塞 UI）；不改 refreshResults()（高频路径附加 5 万行重载不可接受）；验证=构建 0 error + ctest 31/31；已知限制=AutoTask 流水线 region 步骤后无此刷新 | `docs/bugfix/2026-08-21-Bugfix-ProxyListPanel-RegionAutoRefresh-v1.0.md` |
| 2026-08-21 | **RegionBatchResolver 数据源切换 ip-api.com → ipwho.is（Cloudflare anycast 判定差异修复）** — 用户报告 region 解析与 ipinfo.io 基准差异大：3 个 Cloudflare anycast IP 实测 ip-api.com 全部不一致且跨洲偏差（US→CA、LT→TR、US→CA）；横向对比确认根因为 GeoIP 数据库分歧（ip-api.com free 对 Cloudflare 网段 0/3 一致，ipwho.is 3/3 一致），解析代码无缺陷；修复=`fetchRegionFromIpApi()`→`fetchRegionFromIpWhoIs()`：恢复 isIpPattern 域名判断 + DnsCache 预解析（ipwho.is 仅接受 IP）、请求 `https://ipwho.is/{ip}` + setSslVerifyPeer(false)；批量/单代理两调用点同步；parseRegionFromJson 无需修改（ipwho.is 同样返回完整国名 country 字段）；新增 IpWhoIsFailureResponse 测试用例；验证=构建 0 error + RegionBatchResolverTest 6/6 + ctest 31/31；已知限制=已有 Region 数据不自动刷新、ipwho.is 免费档官方限额 10k 次/月 | `docs/bugfix/2026-08-21-Bugfix-RegionBatchResolver-IpWhoIs-Accuracy-v1.0.md` |
| 2026-08-20 | **RegionBatchResolver API 迁移 + 单元测试** — 移除 ipinfo.io Lite API 依赖（需 Token、HTTPS SSL 配置、内联 DNS 解析），迁移至免费 ip-api.com API（无需 Token、原生支持域名/IP、HTTP 协议）；重构 test_proxy_batch_query 测试为 mock 测试（移除真实网络 I/O）；新增 test_region_batch_resolver 单元测试覆盖 parseRegionFromJson() 5 个场景；ctest 31/31 通过 | `docs/bugfix/2026-08-20-Bugfix-RegionBatchResolver-ApiMigration-UnitTest-v1.0.md` |
| 2026-08-20 | **ProxyListPanel Health 列冷启动初始值 0.5 → 0.0** — 未测试代理（`start_count==0`）经贝叶斯平滑 `(stable+1)/(start_count+2)` 得出 0.5 "假健康"；修复=`ProxyListModel::rebuildMaps()` 对 `start_count==0` 强制 `healthMap_[id]=0.0`，`setRunningDurations()` 的 base 与运行 bonus 均纳入 `start_count>0` 条件（冷启动代理运行中也不授予 +0.3 加成）；有历史代理的贝叶斯平滑与 30 分钟运行加成保留；与 `ProxyScorer::compute` 冷启动 `history_score=0.0` 语义对齐；TDD 新增 `ColdStartProxyHasZeroHealth`（含运行中不授予 bonus 断言）；验证=构建 0 error + ctest 30/30；**另提取三列计算逻辑写入报告**（Starts=runtimeMap 直读 / Runtime=历史+心跳整体替换幂等 / Health=贝叶斯平滑+30min 运行加成封顶 1.0；发现 Runtime 排序仅比较历史部分不含运行中时长，候选改进） | `docs/bugfix/2026-08-20-Bugfix-ProxyListModel-HealthColdStartZero-v1.0.md` + `docs/reports/2026-08-20-Report-ProxyListModel-StatsColumns-v1.0.md` |
| 2026-08-18 | **ProxyListPanel 定时刷新异步化（评价列 UI 卡死修复）+ test 目标 comctl32 manifest 修复 + 未启动代理评价空转刷新修复** — ①根因：`refreshHistoryPeriodic()` 每 2 秒 UI 线程全表查询 5.3 万行 + computeBatch + 全量 rebuildMaps + 二次 DB 查询；修复=后台线程独立 sqlite3 连接查 running 时长 → `wxQueueEvent` 投递 `RunningDurationsLoadedEvent`（Events.h/cpp 新增）→ `setRunningDurations` 增量更新 + `notifyHistoryChanged` + `Refresh`；防抖 `refreshInFlight_`；定时器 2000→3000ms；`AppController::getRunningDurationsAsync` 实现（复用 loadProxiesAsync 连接模式）；网络监控保持 2s 不动；②`test_proxy_list_model.exe` 0xC0000139：wxcore 3.3.3 导入 comctl32 v6-only 导出（GetWindowSubclass 等 4 符号），test exe 无 manifest → 加载 system32 comctl32 5.82 → 入口点缺失崩溃；validproxy 有 manifest（icons.rc 内嵌 app.manifest）正常；修复=`tests/test_proxy_list_model.rc`（`1 24 "src/ui/app.manifest"`）追加目标源列表；③未启动代理评价空转刷新：`onRunningDurationsLoaded` 收到空 map 仍无条件重绘 + `setRunningDurations` 用 `+=` 累加绝对值致 Runtime 膨胀；修复=`runningDurations_` 成员整体替换语义 + 返回 changed + 仅 changed 时刷新；验证=ProxyListModelTest 6/6 + ctest 29/30（NetworkMonitorTest 环境依赖失败：example.com 不可达，与本次无关） | `docs/specs/2026-08-18-Spec-ProxyListRefresh-v2.0.md` + `docs/bugfix/2026-08-18-Bugfix-TestExe-Comctl32-Manifest-v1.0.md` + `docs/bugfix/2026-08-18-Bugfix-ProxyListRefresh-IdleRedraw-v1.0.md` |
| 2026-08-18 | **构建依赖迁移 E:/vcpkg → D:/vcpkg + Boost 改 vcpkg 管理 + wxWidgets 3.3.3 适配** — CMakeLists/CMakePresets VCPKG_ROOT 默认 D:/vcpkg、删除 BOOST_ROOT；9 处 boost_json 库路径 mgw14→gcc14（`libboost_json-gcc14-mt-x64-1_91.a`）；`copy_wx_dlls()` 更新 3.3.3 DLL 命名并给 test_log_statistics_event 追加；ConfigDialog 4 处 `wxPG_FILE_DIALOG_TITLE`→`wxPG_DIALOG_TITLE`；zlib 显式 `-DZLIB_LIBRARY=libzs.a`；配置命令须显式 gcc + 覆盖 CMAKE_CXX_FLAGS_DEBUG + `-O coff`；验证=构建 464 目标、ctest 29/29、CLI -h 正常 | `docs/bugfix/2026-08-18-Bugfix-Build-Deps-VcpkgMigration-v1.0.md` |
| 2026-08-17 | **Standalone 代理 watch 生命周期完善（查重前置 / 心跳 / 悬垂纳管）** — ①查重前置：UI 层 `ProxyListPanel::onStartProxy` 在端口检查前调 `AppController::isStandaloneProxyRunning`（`standalone_<indexId>-xray.json` 同名 config 探测）拦截重复启动，`startStandaloneProxy` 内部查重同步提前至 profile 获取后；②watch 心跳：`ProcessExitListener` 新增 `HeartbeatFn` 回调 + watch 线程改 30s 循环等待（WAIT_OBJECT_0 走原退出逻辑 / WAIT_TIMEOUT 调心跳 / WAIT_FAILED 与 !running break），`ProxyRuntimeHistoryDAO::touchHeartbeat` 定期刷新进行中会话 duration_ms（不改 ended_at，聚合仅 finalizeStop 累加），AppController 初始 watch 与 R6 takeover 内 watch 均接入；③悬垂进程纳管：`ProcessInspector::extractConfigFileName`（命令行提取 standalone_*.json）+ DAO `findInProgressHistory`（复用未结束会话）+ `AppController::adoptDanglingStandaloneProxies`（MainFrame 构造后自动枚举 xray/sing-box 残留进程纳管：历史会话 + watch + 心跳）；验证=98/98 构建 + 实机 PID 4084/5544 自动纳管 + taskkill 后 finalizeStop 自动收尾（ended_at/exit_code=1/duration_ms≈31s） | `docs/bugfix/2026-08-17-Bugfix-StandaloneProxy-WatchLifecycle-v1.0.md` |
| 2026-08-17 | **合法 proxy 配置误报 "has wrong type" 伪警告修复** — `ProxyConfigParser.h:95` 警告误放在 `if (obj.contains("proxy") && is_object())` 分支内部（合法 proxy 对象也触发伪警告），且错误类型时消息缺 `(expected object), using default` 后缀（其余 9 个解析器均正确）；修复=警告移至 `else if (obj.contains("proxy"))` 分支并补全后缀；TDD 新增 2 回归测试（`ProxySectionValidObject_NoWrongTypeWarning` / `ProxySectionWrongType_UsesDefaultAndWarns`），RED→GREEN 验证；test_config_reader 36/36、ctest ConfigReaderTest 100% passed | `docs/bugfix/2026-08-17-Bugfix-ProxyConfigParser-WrongTypeWarning-v1.0.md` |
| 2026-08-12 | **About 窗口编译时间修复（三次修复）** — `update_version_h` ALL target + `OBJECT_DEPENDS` 机制已正确，但源码树残留 stale `include/version.h`（08/07/2026 16:36:03）被 `include_directories(${CMAKE_CURRENT_SOURCE_DIR}/include)` 优先于 `build/include/version.h` 检出，导致编译单元嵌入旧时间戳；修复=删除源码树 `include/version.h`（`.gitignore` 已覆盖）；验证=exe 内嵌唯一时间戳 `2026-08-12 17:39:19`、旧时间戳消失、ctest 24/24 | `docs/bugfix/2026-08-07-Bugfix-GUI-AboutBuildTime-LogLevelSync-v1.0.md` |
| 2026-08-12 | **shadowsocks-2022 出站 gRPC 注入编码修复** — legacy `xray.proxy.shadowsocks.ClientConfig`/`Account` cipher 枚举无 2022-blake3 值 → Xray 拒 `unsupported shadowsocks cipher method`、SS2022 批量测试全失败；修复=src/XrayApi.cpp 编码层 Method A：`isShadowsocks2022` 特征标志（parseOutboundJson shadowsocks 分支按 `method.rfind("2022-",0)==0` 选型）+ 新编码分支按 `xray.proxy.shadowsocks_2022.ClientConfig` 扁平结构（field1 address / 2 port / 3 method / 4 key=password 原样，无 Account/ServerEndpoint 嵌套，空 method/key 回退子进程路径）+ `supportedTypeUrls[]` 注册 + 空配置服务端类回退检查；无公共 API 变更、legacy SS 路径不变；新增 7 项 XrayApiDirectTest 单测（wire 格式 + 回退 + legacy 回归守卫）；构建 347/347、ctest 24/24 | `docs/bugfix/2026-08-12-Bugfix-Ss2022-XrayApi-Protobuf-v1.0.md` |
| 2026-08-12 | **日志统计原因归一化聚合（去具体 host）** — 统计弹窗 WARN/ERROR 原因 521 类全因按含 host 消息原文聚合拆类（`SKIP: 140.248.186.45:443 - invalid UUID format` 每 host 一类）；根因=`parseLogFile` 按 `++warnAgg[message]`/`++errorAgg[message]` 原文聚合；修复=src/LogStatistics.cpp 新增 `extractReason` 归一化聚合键（`SKIP: <host>[:port] - <reason>` → `<reason>` 去首尾空白；无 ` - ` 分隔符/非 SKIP 前缀保留原文，如 `Failed to parse vmess: not JSON`、`unsupported SS cipher: 'aes-256-cfb'`）+ WARN/ERROR 分支聚合键改 `warnAgg[extractReason(message)]`/`errorAgg[extractReason(message)]` + ReasonCount 注释同步；标题行格式 `===== WARN 原因明细 (N 类) =====` 已具备；LogStatisticsTest 10 用例、构建 347/347、ctest 24/24 | `docs/bugfix/2026-08-12-Bugfix-LogPanel-Statistics-ReasonAggregation-v1.0.md` |
| 2026-08-13 | **updateTestResultBatch 外层事务嵌套导致 SQLite 报错** — `updateTestResultBatch()` 对每个子批次无判断直接 `BEGIN TRANSACTION`，当调用方已在同一 `sqlite3*` 连接上开启外层事务时触发 `cannot start a transaction within a transaction`；修复=调用 `sqlite3_get_autocommit(execDb)` 判断当前连接是否处于 autocommit，仅在外层无事务时才 `BEGIN`，且仅对自身开启的事务执行 `COMMIT`/`ROLLBACK`，安全参与外层事务；回归测试 `UpdateTestResultBatch_SucceedsInsideOuterTransaction` 覆盖外层 `BEGIN` → 调用 → 数据验证 → 外层 `ROLLBACK` 不残留；构建 347/347、ctest 24/24 | `docs/bugfix/2026-08-13-Bugfix-ProfileExItemDAO-NestedTransaction-v1.0.md` |
| 2026-08-12 | **LogPanel 日志统计按钮关闭弹窗后无法再次打开** — `std::thread::joinable()` 线程结束后仍 true 直至 join；`onLogStatistics` 防叠加守卫首次点击后恒拦截、`onLogStatisticsResult` 从不回收线程；修复=结果回调开头 `if (statsThread_.joinable()) { statsThread_.join(); }`；解析中重复点击仍防叠加；构建 347/347、ctest 24/24 | `docs/bugfix/2026-08-12-Bugfix-LogPanel-Statistics-Reopen-v1.0.md` |
| 2026-08-12 | **LogPanel 清空按钮文案调整 + 异步日志统计弹窗** — Clear 文本改「清空日志窗口」；新增「日志统计」按钮（ID_LOG_STATISTICS）异步解析当前日志文件（`parseLogFile` 纯 std 解析器 + std::thread + wxQueueEvent 投递 `LogStatisticsEvent`），弹窗输出 TRACE/DEBUG/INFO/REPORT/WARN/ERROR 六级别计数，WARN/ERROR 额外输出原因（消息原文）及次数（降序）；工具栏顺序=清空日志窗口→日志统计→打开日志；析构 join 防悬垂；LogStatisticsTest 8 用例；构建 348/348、ctest 24/24 | `docs/specs/2026-08-12-Spec-LogPanel-Statistics-v1.0.md` |
| 2026-08-12 | **LogPanel 工具栏新增「打开日志」按钮 + 移除状态栏双击打开** — Clear 后新增「打开日志」按钮（`Logger::getFilePath()` 实时取路径，ShellExecuteA/wxLaunchDefaultApplication 打开）；LogPanel 事件表由 `EVT_BUTTON(wxID_ANY)` 改为具体 ID（ID_LOG_CLEAR/ID_LOG_OPEN/ID_LOG_FILTER，消除新增按钮后 Clear 被重复触发陷阱）；MainFrame 删除整条状态栏 `wxEVT_LEFT_DCLICK` 双击绑定（原双击 field2/3 也打开日志的误触发缺陷）及 `onStatusBarDClick`/`logFilePath_`；状态栏 Field1 日志文件名显示保留；构建 341/341 | `docs/specs/2026-08-12-Spec-LogPanel-OpenLogButton-v1.0.md` |
| 2026-08-12 | **ProfileExItem message 字段排序（最近活跃时间）** — Message 列排序键 = 测试时间与启动时间中较新者（`ProfileExItemDAO::messageActiveTime`/`compareMessage` 静态方法，严格 19 字符校验，空/遗留值排最后）；`ProxyListModel::Compare` COL_MESSAGE 接入；新增排序单测 | `docs/specs/2026-08-12-Spec-ProfileExMessage-Sort-v1.0.md` |
| 2026-08-11 | **ProfileExItem message 双时间戳改造** — message 仅写入两类信息：测试成功时间 + 代理启动时间（用户要求恒含 '+' 分隔，测试时间恒在前）；`ProfileExItemDAO::formatTestMessage/formatStartupMessage/currentTimeString/updateStartupTime` 新增（严格 19 字符 yyyy-MM-dd HH:mm:ss 校验，旧值 OK/FAILED/curlMsg/NOT_TESTED 整体替换，仅替换自身一侧保留另一侧）；`updateTestResult`/`updateTestResultBatch` 成功写 `<测试时间>+` 并保留原启动侧、失败不再覆盖 message；AppController::startStandaloneProxy 启动成功后 `updateStartupTime(indexId)`；SubitemUpdaterV2 导入初始 message "NOT_TESTED"→空；UI 无需改动（仅展示）；新增 ProfileExItemDAOTest 17 用例 | `docs/specs/2026-08-11-Spec-ProfileExMessage-v1.0.md` |
| 2026-08-11 | **SS 加密算法白名单扩充 chacha20-ietf-poly1305（含日志分析报告）** — `ui_20260811_112935.log` 去重清洗 CONFIG_ERROR 14146 条分析（invalid UUID format 6927 / unsupported SS cipher 6020 / private/invalid address 1199；判定依据 `Deduplicator::deduplicateConfigErrorPhase` 5 重检查）；其中 6018 条 `chacha20-ietf-poly1305`（SS 标准 IETF AEAD）被白名单误拒；修复=`isSupportedSsCipher` 白名单新增第 9 项（src/Utils.cpp line 441），Phase 4/6 清洗与导入闸门自动生效；`aes-256-cfb`（废弃 CFB）维持拒收；构建 327/327、ctest 22/22 | `docs/bugfix/2026-08-11-Bugfix-SsCipher-Whitelist-v1.0.md` |
| 2026-08-11 | **Reality SpiderY nil 越界 panic 致 Xray 实例崩溃修复** — gRPC 注入路径 `encodeRealitySettings` 手工编码遗漏 protobuf field 27（spider_y）→ xray-core 接收端 `SpiderY` nil 切片；REALITY 握手验证失败（批量测试死节点）进入 spider goroutine 访问 `config.SpiderY[0..9]`（reality.go:273）越界 panic 整个 xray 进程崩溃；修复=新增 `encodePackedInt64Field` + `parseSpiderYParams`（镜像 transport_internet.go p/c/t/i/r→槽 0/2/4/6/8）+ `encodeRealitySettings` 无条件编码 10 元素全 0 spider_y；3 新回归测试；构建 327/327、ctest 22/22；**U4（ProxyBatchTester REALITY+gRPC 降级 skip）已随之移除**；上游补丁（reality.go `len(SpiderY)<10` 守卫）列入后续 | `docs/bugfix/2026-08-11-Bugfix-Reality-SpiderY-nil-Panic-v1.0.md` |
| 2026-08-10 | **订阅导入数据污染治理修复计划（draft，待评审）** — 依据根因分析 §4.7/§6 P1：私网地址 42 条 0.014% 来自上游聚合源；三层拦截 U1 utils 纯函数（isPublicAddress 数值判定 172.16-31 防误伤 Cloudflare、isValidUuid 8-4-4-4-12 结构校验、isSupportedSsCipher AEAD+2022-blake3 白名单）→ U2 SubitemUpdaterV2::isValidProxy 导入闸门 → U3 Deduplicator::deduplicateConfigErrorPhase 存量清洗 → U4 ProxyBatchTester::preGenerateConfigs 显式预过滤（pregenFailedFlags_）；ShareLink 仅导出排除；范围不含注入链路 | `docs/plans/2026-08-10-Plan-ImportProxyValidation-v1.0.md` |
| 2026-08-10 | **addOutboundDirect 注入错误配置根因分析** — `bin/log/ui_20260810_113211.log` 批次③ 2715 条失败 2266：A1 id 非法 UUID（hex `U+002D`/`U+0054`，L26-40/86-90/96-100）、A2 双重 URL 编码明文 ID（Parashahonam-299 / Telegram-@Cooonfig@Cooonfig，L133-142）、B SS Unsupported cipher（最高频，Xray v26 仅 AEAD+2022-blake3，L41-85/91-95/128-132/185-214）、C1 11:49:15 REALITY+gRPC panic（reality.go:273 越界，socks=10000/api=10080）、C2 12:04:08 splithttp nil-request panic（config.go:296←client.go:63←dialer.go:481，socks=10001/api=10081，修复前 build）、D 11:49:13 重启窗口内 `xray api ado` 回退失败；结论=注入编码无缺陷，根因为数据污染（A+B）+ Xray-core 运行时缺陷（C）；建议 P1 导入期 UUID/cipher 校验+存量清洗、P2 REALITY+gRPC 调度层规避、P3 回退重试；§4.7 私网地址调查：5323085616270219902=vmess/127.0.0.1:1080/伪 UUID（Argh94-vmess 订阅），上游文件 base64 解码逐字段命中（同 id/端口/备注），全库私网 42 条（0.014%，10 订阅源），源码 14 处 127.0.0.1 全为本地绑定→地址污染来自上游；P1-c 增补地址合法性校验 | `docs/bugfix/2026-08-10-Analysis-InjectionError-ConfigRootCause-v1.0.md` |
| 2026-08-10 | 重启实例继续服务后续队列项 — 实例崩溃经 evaluateInstanceHealth 成功重启后 worker 循环顶 `if (instanceDead.load()) break;` 且 instanceDead 从不复位 → worker 退出、有效并发永久减一；修复=循环顶改为 `if (instanceDead && relaunchedOk) { WARN 日志; instanceDead.store(false); } else if (instanceDead) break;`——重启成功复位继续服务剩余队列项，仅重拉失败才退出；构建 309/309、ctest 22/22 | `docs/bugfix/2026-08-10-Bugfix-RelaunchedInstanceContinuesQueue-v1.0.md` |
| 2026-08-10 | SplitHTTP nil-request Panic 应用侧防御 — Xray-core splithttp OpenStream 丢弃 `NewRequestWithContext` 错误，host/path 含控制/空白字符（≤0x20 或 0x7F）时 url.Parse 失败返回 (nil,err) → FillStreamRequest 对 nil request 解引用 panic（commit d2758a0 验证）；修复=addOutboundDirect 注入前 validateSplitHTTPSettings 校验 streamSettings（splithttp/xhttp 的 host/path 禁控制字符），非法代理 ERR 拒绝注入不再崩溃；空 host 放行（dest.Address 兜底）；7 例单测 | `docs/bugfix/2026-08-10-Bugfix-SplitHTTP-nil-request-Panic-v1.0.md` |
| 2026-08-10 | Xray 实例崩溃观测增强（stderr/退出码捕获）— gRPC 注入期间 xray 进程死亡但 panic 堆栈不可见（CreateProcessW 无 STARTF_USESTDHANDLES、无日志文件）；修复=start() 重定向 stdout/stderr 至 `<configDir>/xray_stdout_<socksPort>.log`/`xray_stderr_<socksPort>.log` + 死亡时 ERR 日志记录退出码与 stderr 尾部 4KB + lastExitCode()；测试=编译型假 xray fake_xray_helper（.bat 因 CreateProcessW 不执行体被弃用），XrayInstanceStderrTest 2/2 通过（运行期死亡 exitCode=42 + stderr tail PANIC / 启动期死亡 exitCode=7） | `docs/bugfix/2026-08-10-Bugfix-XrayInstance-StderrCapture-v1.0.md` |
| 2026-08-10 | Xray 实例崩溃/挂死自动恢复 + gRPC 错误日志提升 ERROR — 连续两次 grpcConnect 失败触发 connectFailureHook → worker 停止重试 → evaluateInstanceHealth 释放资源（stop+freePort+erase）后用原配置（same socksPort→same config file）两阶段锁外拉起；XrayApi 5 处 FAILED 路径 + worker XRAY_ERROR 日志 DEBUG→ERR；在途代理重拉成功后重试一次（injectError 提前保存修复空错误消息）；构建 302/302、ctest 21/21 | `docs/bugfix/2026-08-07-Xray-instances-are-not-automatically-restarted.md` |
| 2026-08-07 | 状态栏 Field0 被菜单清空 — wxFrameBase::DoGiveHelp 菜单打开时空 help 清空 Field0、点击关闭不恢复（ESC 可恢复）；修复=initStatusBar 后 SetStatusBarPane(-1) 禁用帮助机制 + doTestAllProxies/doTestSingleProxy 启动发 "Testing..." 状态文本 + 移除 dumpStatusBarGeometry；验证菜单全链路 part0 恒定 | `docs/bugfix/2026-08-07-Bugfix-MainFrame-StatusBar-Field0-v1.0.md` |
| 2026-08-07 | git-master 技能安装 — AGENTS.md §6.2 路由引用但环境缺失；GitHub 直连不通致 npx skills add 失败，配置 git 全局代理 socks5://127.0.0.1:10808 后手动 clone josiahsiegel/claude-plugin-marketplace 复制至全局技能目录（10 文件），重启 Kilo 后生效 | `docs/reports/2026-08-07-Report-GitMasterSkill-Install-v1.0.md` |
| 2026-08-07 | About 窗口编译时间不准确 — version.h 生成依赖 build.ninja 致仅 configure 时刷新（增量构建 APP_BUILD_TIME 停留上次 configure 时刻），改 add_custom_target(update_version_h ALL) 每次构建重生成；配置窗口保存 console 日志级别未同步 LogPanel 下拉框过滤，保存回调补 setInitialLogLevel | `docs/bugfix/2026-08-07-Bugfix-GUI-AboutBuildTime-LogLevelSync-v1.0.md` |
| 2026-08-07 | 订阅更新跳过提示误报 ERROR — updateAll() 全部订阅因更新间隔被跳过时以 ERR 输出 "All subscriptions skipped by update interval - nothing to update"（非错误，随后 return true 正常完成）；修复=ERR→REPORT；真正失败的 "failed to update" 保持 ERR | `docs/bugfix/2026-08-07-Bugfix-SubitemUpdater-SkipLogLevel-v1.0.md` |
| 2026-08-07 | 网络监控探测错误详情日志级别过低 — CheckURLWithDnsFlag 内 probe failed/DNS error/http_code 从 DEBUG/TRACE 提升为 WARN（生产 console_level=WARN 下故障可见）；ThreadLoop 状态机日志保持 ERR | `docs/bugfix/2026-08-07-Bugfix-NetworkMonitor-ProbeLogLevel-Warn-v1.0.md` |
| 2026-08-07 | GUI 启动日志未按 config.json file_level 过滤 — main_gui.cpp Logger::init 前预解析 log 段（ConfigFileStore+ConfigJsonParser+LogConfigParser），file_level=ERROR 时 INFO/DEBUG 不再写入日志文件 | `docs/bugfix/2026-08-07-Bugfix-GuiLogFileLevel-v1.0.md` |
| 2026-08-06 | 状态栏日志文件名不可见 — netMonPanel_ 遮挡 Field1；状态栏扩 4 字段 + SetStatusWidths + 双击 Bind 状态栏 | `docs/bugfix/2026-08-06-Bugfix-MainFrame-StatusBar-LogFile-v1.0.md` |
| 2026-08-06 | GUI 入口 Logger 启动级别补全 console_level + LogPanel 筛选器同步 config.json | `docs/bugfix/2026-08-06-Bugfix-Logger-GUI-ConsoleLevel-v1.0.md` |
| 2026-06-09 | xray.executable 配置值校验 — 加载时 ERROR+弹窗、GUI 文件存在性+扩展名检查 | `docs/superpowers/specs/2026-06-09-Spec-Validproxy-xray-executable-validation-v1.0.md` |
| 2026-06-09 | test_config_reader.exe 输出目录从 bin/ 移至 tests/ — 遵循测试文件目录规范 (AGENTS.md #8) | `CMakeLists.txt` |
| 2026-06-09 | Config validation improvements plan — 5 tasks: in-class initializers, type-warn logs, xray existence check, save failure notification, load() unit tests | `docs/plans/2026-06-09-config-validation-improvements-plan.md` |
| 2026-06-09 | UI 布局调整 — 工具栏去除 dbpath 显示 / searchbox 右移 50px / ProxyDetail 默认隐藏 | `docs/superpowers/specs/2026-06-09-Spec-Validproxy-ui-layout-tweaks-v1.0.md` |
| 2026-06-09 | 订阅列表面板 — 新增"有效"列 (delay>0) + 列名中文化（名称/有效/代理数/更新） | `docs/superpowers/specs/2026-06-09-Spec-Validproxy-subscription-column-i18n-and-valid-count-v1.0.md` |
| 2026-06-09 | Resizable splitter for subscription/proxy panels — wxSplitterWindow implementation with parent fix | `docs/plans/2026-06-09-feat-ui-resizable-splitter-v1.0.md` |
| 2026-06-04 | SubitemUpdaterV2 硬编码 `"bin/config"` 路径修复 | `docs/bugfix/2026-06-04-subitemupdater-hardcoded-binconfig-path.md` |
| 2026-06-12 | Worker join 超时修复 v3 — 动态 join timeout + 轻量 warmup（v2 XrayApi::ping 轮询过重，v1 7s 不足） | `docs/bugfix/2026-06-12-Bugfix-ProxyBatchTester-Worker0-JoinTimeout-v1.0.md` |
| 2026-06-15 | AutoTask 管道诊断不可见修复 — SubitemUpdaterV2 return false + 日志级别低 + step name 别名缺失 + ConfigDialog 改进 + ProxyBatchTester zero-proxy return true | `docs/bugfix/2026-06-15-Bugfix-AutoTask-SubitemUpdater-logging-and-pipeline.md` |
| 2026-06-15 | NetworkMonitor 批量网络中断中止 — 独立 NetworkMonitor 类，后台线程 HEAD 探测，auto atomic 标志，ProxyBatchTester/SubitemUpdaterV2 检测中断自动中止 | `docs/superpowers/specs/2026-06-15-Spec-NetworkMonitor-batch-network-abort-on-disconnect-v1.0.md` |
| 2026-06-16 | **Code Review 全量修复: #1 CLI Ctrl+C / #2 状态文件路径 / #3 SQL(abandon) / #4 未用图标 / #5 重复方法合并 / #6 版本验证 DONE** + 额外修复: NetworkMonitor 启动虚假 RESTORED (firstCheckDone_), MainFrame re-entry guard (5 handlers), REALITY publicKey ERR→INFO, onUpdateType→updateMethod 重命名, "任务完成通知"移至通知分类 | `docs/bugfix/2026-06-16-Bugfix-Review-AutoTask-CLI-StateFile-v1.0.md` |
| 2026-06-16 | **修复 deduplicate() 嵌套事务错误** — deduplicate() 已 BEGIN 事务后，deduplicateConfigErrorPhase() 调用 deleteByIndexId() 再次 BEGIN 导致 SQLite 错误 | `docs/bugfix/2026-06-16-Bugfix-Dedup-NestedTransaction-v1.0.md` |
| 2026-06-18 | SubitemUpdaterV2.cpp 2422→1198 行重构 — 提取 SubscriptionParser/Deduplicator/Importer/SubscriptionUpdater 四个类 | `docs/specs/2026-06-18-Spec-SubitemUpdaterV2-Decomposition-v1.0.md` |
| 2026-06-25 | **NetworkMonitor 断连探测机制** — 断连后暂停测试，按配置阈值连续探测 N 次后再决定是否终止。修改: ConfigReader (probe_on_disconnect 结构体)、NetworkMonitor (consecutiveFailures_ 延迟取消)、ProxyBatchTester (waitForNetworkRecovery 暂停等待) | `docs/specs/2026-06-25-Spec-NetworkMonitor-ProbeOnDisconnect-v1.0.md` |
| 2026-06-26 | **SubitemUpdaterV2 未传入 netMon_** — doUpdateSubscription/doUpdateAllSubscriptions 构造 SubitemUpdaterV2 时未传第7参数 netMon (默认 nullptr)，6处 IsConnected() 空指针短路永不触发 | `docs/bugfix/2026-06-26-Bugfix-SubitemUpdaterV2-MissingNetMon-v1.0.md` |
| 2026-07-01 | **Sync 目标库新订阅 enabled 默认 0** — migrateSubscription() 直接复制源库 enabled 状态，目标库新订阅应默认禁用 | `docs/bugfix/2026-07-01-Bugfix-Sync-Subscription-EnabledDefault-v1.0.md` |
| 2026-07-01 | **SubscriptionPanel/ProxyListPanel 双击无反应** — MSW wxDataViewMainWindow 缺失 CS_DBLCLKS，使用 selection-change-based 双击检测绕过限制 | `docs/bugfix/2026-07-01-Bugfix-DoubleClick-V1.0.md` |
| 2026-07-01 | **对话框屏幕居中** — 6 处 wxDialog/wxMessageDialog 添加 CentreOnScreen() 调用 | `docs/specs/2026-07-01-Spec-DialogCentering-v1.0.md` |
| 2026-07-02 | **Sing-box Support Implementation** — Design spec created; add sing-box as alternative proxy core to Xray with config fields, UI integration, outbound format mapping | `docs/specs/2026-07-02-Spec-Singbox-Support-v1.0.md` |
| 2026-07-03 | **echconfiglist 双格式解析** — 区分 Base64 ECH 配置与 DNS URL 格式，生成正确 sing-box ech.config；计划文档已创建 | `docs/superpowers/plans/2026-07-03-echconfiglist-dual-format-support.md` |
| 2026-07-09 | **DNS 缓存解析模块 DnsCache** — DnsCache 类，静态 resolve()，getaddrinfo IPv4 + 惰性 WSAStartup + 互斥锁保护的 unordered_map 缓存；集成至 RegionDetector Strategy 3；18/18 ctest 通过 | `docs/specs/2026-07-09-Spec-DnsCache-v1.0.md` |
| 2026-07-20 | **GUI 入口缺失 curl_global_init() 崩溃** — main_gui.cpp 未调用 curl_global_init()，右键解析代理地区时线程内 curl_easy_perform 访问违例崩溃 | `docs/bugfix/2026-07-20-Bugfix-CurlGlobalInit-GUI-v1.0.md` |
| 2026-07-29 | **gRPC addOutboundDirect protobuf 字段 + 路径修复** — TypedMessage field 2→3 (proxy_settings)、两处 gRPC 路径 CommandService→HandlerService 修正、4 单元测试 | `docs/bugfix/2026-07-29-Bugfix-XrayApi-gRPC-AddOutboundDirect-Fields-v1.0.md` |
| 2026-07-29 | **addOutboundDirect JSON parse 回退修复** — ConfigGenerator 输出 `{"outbounds":[{...}]}` 但代码读取 `rootObj["outbound"]` 单对象导致解析抛异常触发回退；提取 parseOutboundJson 静态方法，协议→typeUrl 映射，10 协议映射单元测试，33/33 tests pass | 同上 |
| 2026-07-31 | **AddOutboundDirect 编码字段修复** — (1) protocol 原值被忽略(恒0)改用配置原值 (2) SenderConfig 字段号 1/3→2/4 (stream_settings/multiplex_settings，以 Xray-core command.proto 为准) (3) encodeStreamConfig port int64 被 is_uint64 守卫静默丢弃，扩展 int64>0 编码；StreamSettingsBuilder 复核无缺陷；新增 10 单测 (EncodeMultiplex/Stream/SenderSettings 系列)，44→54，54/54 pass | `docs/bugfix/2026-07-29-Bugfix-XrayApi-gRPC-AddOutboundDirect-Fields-v1.0.md` |
| 2026-08-04 | **代码审计优化 Phase 1-4 全部落地 + CurlEasyHandle 修复** — A1-A6/B1-B14/C1-C9/D1-D8/E1-E14（C2/C5/C8/C9/E3/E4/E8/E9b 验证已实现）；CurlEasyHandle: C6 移动语义补全 cancelFlag_/secondaryCancelFlag_、E3 超时兜底（仅 0/负值→1000ms，修正误伤 NetworkMonitor 快速探测 25ms 的回归）、E4c 二级取消标志 (setCancelFlags/isCancelRequested)；SubscriptionUpdater/SubitemUpdaterV2 fetchUrl TLS 验证恢复、fetchUrlViaProxy 保留豁免+注释；CreateProcessW 迁移；PortManager findAvailable 重写 (45.7s→1.4s)；21/21 测试通过 | `docs/reports/2026-08-04-Report-CurlEasyHandle-Audit-Fixes-v1.0.md` |
| 2026-08-04 | **批量测试网络监控误报断网修复** — waitForNetworkRecovery 无超时 CV 死等（NetworkMonitor 从不 notify networkCv_）→ 一次瞬时探测失败致所有 worker 永久挂起或 3 周期后误取消整批；重写为 250ms 轮询 + 30s 上限；另清理 fc15eec 遗留每代理 2 次 listOutboundsDirect + outbound_json TRACE 调试残留；21/21 测试通过 | 本会话（未建 bugfix 文档） |
| 2026-08-04 | **订阅解析乱码 Shadowsocks 节点修复** — Argh94-ShadowSocks 订阅 malformed ss:// 链接（VLESS 参数伪装成 ss、userinfo 为 URL 编码用户名）+ decodeBase64 非法字符解码为索引 0 → 全库 29811 个代理 Security/Id 二进制垃圾致批量测试全失败（Success:0）；修复：decodeBase64 跳过非法字符、ss:// 分支 isPrintableAscii 校验丢弃、Deduplicator::deduplicateConfigErrorPhase 扩展删除非法配置代理（CLI -D/UI 去重触发）、日志降噪（XRAY_ERROR 不再打印完整 outbound JSON 消除密码泄露、失败日志降 DEBUG）；21/21 通过 | `docs/bugfix/2026-08-04-Bugfix-SubscriptionParser-GarbageSS-v1.0.md` |
| 2026-08-04 | **gRPC xhttp 传输编码与 Xray v26.2.4 兼容修复 + REALITY bytes 编码修复** — 新构建首次真正编码 xhttp（protocolName="xhttp" + `xray.transport.internet.xhttp.Config`）但 v26.2.4 已移除 xhttp 协议（消息类型未注册）→ AddOutbound `proto: not found`；旧构建为静默退化裸 TCP 假成功，非代码回退；修复 network=="xhttp" 统一按 splithttp 编码（protocolName="splithttp" + `xray.transport.internet.splithttp.Config`）；REALITY publicKey/shortId 字段号 23/24 为 BYTES，必须 base64/hex 解码后 encodeLengthDelimited；端到端 207 节点：XRAY_ERROR 8→0（8/8 xhttp 注入成功），3/8 xhttp 连通（cloudflare.182682.xyz/media-ru7/second.shadydomain），88→91 提升，单测 92/92 | `docs/bugfix/2026-08-04-Bugfix-XrayApi-XHTTP-SplitHTTP-Mapping-v1.0.md` |
| 2026-08-05 | **DNS 解析永久缓存 + NetworkMonitor 第二层缓存删除** — DnsShareCache 程序级共享（CURLSH + CURLOPT_DNS_CACHE_TIMEOUT=-1，7 模块复用，进程生命周期）；"统一为单一缓存"调查结论维持两套职责隔离缓存（DnsShareCache 管 libcurl 连接域名、utils::DnsCache 管节点域名）；NetworkMonitor dnsCache_（host→成功时间戳，只写不读死代码）整体删除（成员/include/getHostFromUrl/写入块），dnsFailures_/getDnsFailures() 保留；NetworkMonitorTest 11 用例通过（8.22s） | `docs/specs/2026-08-05-Spec-DnsCache-Permanent-v1.0.md` |
| 2026-08-05 | **导入订阅稽核：去除无法正确生成配置的代理** — utils::isPrintableAscii 提取（Deduplicator/SubscriptionParser 复用）+ SubitemUpdaterV2::isValidProxy 增加 Security/Id 可打印 ASCII 稽核（与去重阶段 deduplicateConfigErrorPhase 判定一致，入库前丢弃二进制垃圾节点）；trojan/hy2 security 空串不误杀；test_utils 新增单测 | `docs/specs/2026-08-05-Spec-ImportProxyValidation-v1.0.md` |
| 2026-08-06 | **代理列表面板测试速度/连接用户数列显示为空** — ProxyListModel/ProxyListPanel 列标题与字符串格式化修复，新增 GetTestSpeedStr/GetUserCountStr 辅助方法 | `docs/bugfix/2026-08-06-Bugfix-ProxyListDisplayValues-v1.0.md` |
| 2026-08-06 | **GUI 入口 Logger 启动级别遗漏 console_level 配置** — main_gui.cpp 补全 setConsoleLevel 调用，与 CLI 一致；LogPanel 筛选器启动级别同步 config.json log_console_level，新增 setInitialLogLevel() 方法；21/21 测试通过 | `docs/bugfix/2026-08-06-Bugfix-Logger-GUI-ConsoleLevel-v1.0.md` |
| 2026-08-06 | **状态栏未铺满底部行** — SetStatusWidths 固定宽度总和 920 逻辑 px < 状态栏逻辑宽 1280 px，右侧空白；末字段改 -1（可变宽度）后 Field3 拉伸铺满，UIA 验证右缘=1280 | `docs/bugfix/2026-08-06-Bugfix-MainFrame-StatusBar-LogFile-v1.0.md` |

